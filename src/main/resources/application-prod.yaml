spring:
  devtools:
    livereload:
      enabled: false
    restart:
      enabled: false
  datasource:
    url: jdbc:h2:file:./db/quiz-db;AUTO_SERVER=TRUE
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update

quiz:
  files-folder: dev/
  profiling: false
  monitoring: false

vaadin:
  productionMode: true